perf-veth-bp
============

Install dependencies:
```
apt install python3-virtualenv gnuplot
```

Setup:
```
./setup.sh
```

Run Server in another terminal:
```
./server.sh
```

Run Tests in one terminal:
```
./tests.sh
```

Results will be written to stdout and and graphs will be in /tmp dirs.
